# Common Commands

## Git
git status
git add .
git commit -m "docs: add command reference notes"
git push origin main

## Docker
docker ps
docker images
docker compose up -d

## Kubernetes
kubectl get pods
kubectl get svc
kubectl get nodes

## Terraform
terraform init
terraform plan
terraform apply
